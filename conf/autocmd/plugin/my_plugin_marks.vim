"
"
"kshenoy/vim-signature
"
"

" 防止重复加载
if get(s:, 'loaded', v:false)
  finish
else
  let s:loaded = v:true
endif
"----------------------------------------------------------------------
"----------------------------------------------------------------------
" 用于在侧边符号栏显示 marks （ma-mz 记录的位置）
packadd vim-signature
" 2}}}
"----------------------------------------------------------------------
